**Findings**
1. High: Master fill handling now overwrites account-level expected state with the quantity of the leg that just filled. [V12_002.Orders.Callbacks.cs:233](C:/WSGTA/universal-or-strategy/src/V12_002.Orders.Callbacks.cs#L233) calls `SetExpectedPositionLocked(...)` on every master fill, but multi-leg master entries reserve expected qty per leg, for example TREND E1 and E2 at [V12_002.Entries.Trend.cs:214](C:/WSGTA/universal-or-strategy/src/V12_002.Entries.Trend.cs#L214) and [V12_002.Entries.Trend.cs:237](C:/WSGTA/universal-or-strategy/src/V12_002.Entries.Trend.cs#L237). After the second leg fills, expected becomes only that leg’s qty instead of the account total, which can produce a false master desync and flatten a valid position.

2. High: The new master-account exit bookkeeping misclassifies non-target fills as targets. [V12_002.Orders.Callbacks.AccountOrders.cs:77](C:/WSGTA/universal-or-strategy/src/V12_002.Orders.Callbacks.AccountOrders.cs#L77) matches any filled master order whose name starts with `T` and contains `_`. That includes real entry names like `TREND_...` and `TrendMnlLong_...` from [V12_002.Entries.Trend.cs:183](C:/WSGTA/universal-or-strategy/src/V12_002.Entries.Trend.cs#L183) and [V12_002.Entries.Trend.cs:397](C:/WSGTA/universal-or-strategy/src/V12_002.Entries.Trend.cs#L397), so normal entry fills will be processed as exits and corrupt `expectedPositions`.

3. High: The same new fill path is not partial-fill safe. [V12_002.Orders.Callbacks.AccountOrders.cs:67](C:/WSGTA/universal-or-strategy/src/V12_002.Orders.Callbacks.AccountOrders.cs#L67) zeroes master expected qty on any `Stop_` `PartFilled`, even if size remains open, and [V12_002.Orders.Callbacks.AccountOrders.cs:79](C:/WSGTA/universal-or-strategy/src/V12_002.Orders.Callbacks.AccountOrders.cs#L79) subtracts cumulative `order.Filled` on every target `PartFilled`/`Filled` callback, which over-decrements on partials. Both paths can create false REAPER desyncs.

4. High: The restart repair is internally inconsistent for master brackets. [V12_002.SIMA.Lifecycle.cs:365](C:/WSGTA/universal-or-strategy/src/V12_002.SIMA.Lifecycle.cs#L365) now adopts master previous-session brackets in `OrderState.Unknown`, but the master naked-position audit still only recognizes `Working`/`Accepted` stops at [V12_002.REAPER.Audit.cs:316](C:/WSGTA/universal-or-strategy/src/V12_002.REAPER.Audit.cs#L316). In the exact restart case this fix targets, REAPER can still decide the master is naked and queue an unnecessary emergency hard stop.

5. Medium: I cannot sign off the FSM promotion while the diff explicitly reintroduces the banned lock path. `stateLock`/`dailySummaryLock` are restored at [V12_002.cs:143](C:/WSGTA/universal-or-strategy/src/V12_002.cs#L143), and `lock (stateLock)` is still used in the replace callback at [V12_002.Orders.Callbacks.AccountOrders.cs:298](C:/WSGTA/universal-or-strategy/src/V12_002.Orders.Callbacks.AccountOrders.cs#L298). That conflicts with the actor-model rule in [AGENTS.md](C:/WSGTA/universal-or-strategy/AGENTS.md) and [.agent/standards_manifesto.md](C:/WSGTA/universal-or-strategy/.agent/standards_manifesto.md).

**Gate**
BLOCK. I also did not find a Director-approved execution artifact for this expanded scope; the only mission file I found is still marked pending at [implementation_plan_993.md:2](C:/WSGTA/universal-or-strategy/docs/brain/implementation_plan_993.md#L2), while the worktree has already moved into Build 1000-1002 changes.

**Verification**
`python check_ascii.py` passed on the touched core files. `dotnet build universal-or-strategy.sln` could not be completed here because NuGet restore to `api.nuget.org` is blocked in this sandbox, so I do not have a compile verdict.